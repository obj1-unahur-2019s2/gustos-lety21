import personas.*
import objetos.*
import bolichito.*

// auto-generated at 29/08/2019 20:01:36
test "Gustos de Rosa" {
	assert.equals(true,rosa.leGusta(remera))
	assert.equals(false,rosa.leGusta(biblioteca))
	placa.peso(3500)
	assert.equals(false,rosa.leGusta(placa))
	
}

test "Bolichito desequilibrado" {
	bolichito.ponerEnMostrador(pelota)
	bolichito.ponerEnVidriera(biblioteca)
	
	assert.equals(false,bolichito.estaDesequilibrado())	
}
